# source_docs

This folder stores raw, source documents for the RAG pipeline.
- Files here are ingested into the vector store.
- Do NOT modify files in-place once the pipeline is running; instead, update the upstream source and re-run ingestion.
